//! Lattice Viterbi / beam-search decoder. CORE agent: implement.
use crate::{Candidate, Engine, EngineConfig};

pub fn decode(_engine: &Engine, _input: &str, _cfg: &EngineConfig) -> Vec<Candidate> {
    Vec::new()
}

pub fn predict(_engine: &Engine, _input: &str, _cfg: &EngineConfig) -> Vec<Candidate> {
    Vec::new()
}
